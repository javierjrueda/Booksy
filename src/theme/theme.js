import {createTheme} from '@mui/material/styles';

export const appTheme = createTheme({
  palette: {
    primary: {
      main: '#0A2D2F',
      opacity: 'rgb(10, 45, 47,0.2)'
    },
    secondary: {
      main: '#FF5622'
    }
  }
});
